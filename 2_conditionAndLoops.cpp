#include <iostream>
using namespace std;

int main(){
    /* int n;
    cout << "Value do n ki: ";
    cin >> n;
    cout << "Yeh rhi n ki value: " << n << endl;

    if(n > 0){
        cout << "N is Positive " << endl;
    }
    else{
        cout << "N is Negative " << endl;
    }
 */
    /*     int a,b;
    // cin >> a >> b;
    a = cin.get();
    cin >> b;
    cout << "Value of a and b: " << a << " " << b << endl; */

    /* int a,b;
    cout << "Enter the value of a and b: ";
    cin >> a >> b;
    if(a>b){
        cout << "A i sgreater" << endl;
    }
    else{
        cout << "B is greated than A" << endl;
    }
 */
    // HOMEWORK ☻
    /* 
    char a;
    cout << "Enter a character a: ";
    cin >> a;
    int s = a;
    if(s > 64 && s < 92)
        cout << "This is Upper Case" << endl;
    else
        cout << "This is Lower Case" << endl; 
*/
    /*
    int n;
    cin >> n;

    int i= 1;
    while(i < n){
        cout << i << " ";
        i += 1;
    }
    */

    // HOMEWORK ☻
    /* int n;
    cin >> n;
    int s = 0;
    int i = 1;
    while(i<n){
        if(i%2 == 0)
        s += i;
        i+=1;
    }
    cout << s << endl;
     */

    cout << "  Farhenhiet  |  Celcius  ";
    double n=100;
    // cin >> n;
    double i = 1;
    while(i<=n){
        cout << "     " <<  i << "      |      "  << (i-32)*5/9 << endl;
        i+=0.1;
    }
// c = (f - 32) * 5/9;0













}